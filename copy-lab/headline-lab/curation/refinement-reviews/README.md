# Refinement reviews

Each independent review selects exactly 15 systems from the 60 `*-refinements` candidates, with at least three picks from each source batch.

```json
{
  "lens": "Editorial lens",
  "picks": [
    { "id": "H931", "score": 9.4, "reason": "A concise editorial reason under sixteen words." }
  ]
}
```
