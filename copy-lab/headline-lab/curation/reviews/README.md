# Independent editorial reviews

Each review file contains a `lens` and a `picks` array with `{ "id", "score", "reason" }`. `build-curation.mjs` requires at least three lenses and produces a diverse 60-item curated set.
